# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=smarttemp
POSTGRES_PASSWORD=smarttemp_secret
POSTGRES_DB=smart_temp_monitoring

# Backend
BACKEND_PORT=4000
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRATION=24h

# SMTP (default)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@smarttemp.local
SMTP_TLS=true

# Redis (for queue)
REDIS_HOST=localhost
REDIS_PORT=6379

# Frontend
VITE_API_URL=http://localhost:4000/api
